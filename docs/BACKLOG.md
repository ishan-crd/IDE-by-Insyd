# Backlog (ideas parked outside the current phase)

- Bundle a pinned Node runtime for npm-distributed ACP adapters (avoids "install Node" step).
- Codex app-server extras: thread fork, `review/start`, rate-limit display.
- Scheduled agent runs (cron-like) — Superset has it; super.engineering does not.
- Mobile/web companion via the socket API behind a relay (T3 Code has it).
- Dev containers.
- Terminal: kitty graphics protocol, sixel.
- Editor: own editor on ropey + tree-sitter if gpui-component's editor blocks the design.
- CEF off-screen browser for a fully composited browser tab.
- Dim Mode (backlight control) from Snor — cute, platform-specific, low priority.
