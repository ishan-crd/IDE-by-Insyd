{
  "name": "insyde-monorepo",
  "private": true,
  "description": "InsyDE: the Rust app (Cargo workspace in crates/) and the web packages (pnpm workspace).",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "site": "pnpm --filter site dev",
    "site:build": "pnpm --filter site build",
    "site:preview": "pnpm --filter site preview"
  }
}
